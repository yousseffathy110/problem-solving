#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    double radius;
    cin >> radius;
    cout << fixed << setprecision(6) << 4 * radius * radius;
}